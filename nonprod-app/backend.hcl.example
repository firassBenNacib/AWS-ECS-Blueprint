# Copy to backend.hcl and customize.
# Keep a unique key per deployment-root/account/environment/workspace.

bucket         = "your-terraform-state-bucket"
key            = "nonprod-app/nonprod/terraform.tfstate"
region         = "eu-west-1"
encrypt        = true
